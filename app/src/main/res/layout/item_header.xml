<!-- Sana sarlavhasini ko‘rsatadigan TextView -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/textHeader"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:textSize="16sp"
    android:textColor="@color/title_color"
    android:textStyle="bold"
    android:gravity="start"
    android:fontFamily="@font/poppins"
    android:layout_marginTop="10dp"
    android:layout_marginBottom="10dp"
    android:paddingVertical="8dp"
    android:paddingHorizontal="16dp"
    android:background="@drawable/bg_note_header" />
    <!-- Matn o‘lchami -->
    <!-- Matn rangi -->
    <!-- Qalin matn -->
    <!-- Matn chapdan joylashadi -->
    <!-- Poppins shriftidan foydalanilgan -->
    <!-- Yuqoridan bo‘sh joy -->
    <!-- Pastdan bo‘sh joy -->
    <!-- Ichki vertikal bo‘sh joy -->
    <!-- Ichki gorizontal bo‘sh joy -->
    <!-- Orqa fon (masalan: rounded background) -->