<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Bu fayl BottomNavigationView uchun menyu elementlarini belgilaydi -->

    <!-- Bosh ekran tugmasi -->
    <item
        android:id="@+id/home_btn"
        android:icon="@drawable/ic_home"
        android:title="Bosh ekran"
        app:showAsAction="ifRoom" />



    <!-- Arxiv bo‘limi tugmasi -->
    <item

        android:id="@+id/archive_btn"
        android:icon="@drawable/ic_archive"
        android:title="Arxiv"
        app:showAsAction="ifRoom" />

    <!-- Bu joyni FAB uchun bo'sh qoldiramiz -->
    <item
        android:id="@+id/empty_btn"
        android:title=""
        app:showAsAction="never" />


    <!-- Sevimlilar tugmasi -->
    <item
        android:id="@+id/favorite_btn"
        android:icon="@drawable/ic_favorite"
        android:title="Sevimli"
        app:showAsAction="ifRoom" />


    <!-- Profil tugmasi -->
    <item
        android:id="@+id/profile_btn"
        android:icon="@drawable/ic_profile"
        android:title="Profil"
        app:showAsAction="ifRoom" />
</menu>
